jQuery.fn.extend({
  imageAttach: function() {
    return this.each(function() {
      // var image_button = $('<div class="image-attach"><div>');
      // var input = $('<input type="file" name="[feed_images_attributes]['+Date.now()+'][image]">');
      // image_button.html(input);

      // image_button.click(function(){
        // this
      // });
      
      // $(this).find('.emojionearea-editor').after(image_button);
    });
  }
});