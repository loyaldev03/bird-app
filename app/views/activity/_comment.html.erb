<p class="feed-posted"> 
  <%= time_ago_in_words(activity['time'].in_time_zone) %> ago.
</p>
  
<%= link_to activity['actor'] do %>
  <%= image_tag activity['actor'].avatar.thumb.url, class: "feed-avatar" %>
  <%= activity['actor'].name %>
<% end %>
<em>
  has commented on
</em>
<% if activity['object'].class == User %>

  <% if activity['foreign_id'].parent.present? %>
    <%= link_to activity['object'] do %>
      <%= activity['object'].name + '\'s' %>
    <% end %>
    
    <em>comment</em>

    <% if activity['foreign_id'].parent.try(:body).present? %>
      <%= link_to activity['foreign_id'].parent.body.truncate(20), activity['foreign_id'] %>
    <% else %>
      <%= link_to activity['foreign_id'].parent.text.truncate(20), activity['foreign_id'] %>
    <% end %>
    
  <% else %>
    <em>his page</em>
    <%= link_to activity['foreign_id'].body.truncate(20), activity['foreign_id'] %>
  <% end %>

<% else %>

  <%= link_to activity['object'].try(:body).try(:truncate, 20), activity['object'] %>

<% end %>

<hr class="feed-divider">