<% renderer = (@feed == 'user' || @feed == 'timeline') ? "aggregated_activity/userpage_#{ @new_item }" : "activity/commentable_#{ @new_item }" %>

$('.feed-block').prepend(`
    <%= escape_javascript(
      render(partial: renderer, 
        locals: { activity: @activity } )
    ) %>
  `);
