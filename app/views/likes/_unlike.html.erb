<% if style == 'release' %>

  <%= link_to like_path(like, style: style), 
        id: "like-#{like.likeable_type}-#{like.likeable_id}",
        method: :delete, remote: true, class: "like-button" do %>
    <%= 'YOU LIKED IT' %>
    <%= image_tag 'icons/liked-release.svg' %>
  <% end %>

<% else %>

  <%= link_to like_path(like, style: style), 
      id: "like-#{like.likeable_type}-#{like.likeable_id}",
      method: :delete, remote: true, class: "like-button" do %>
    Unlike
  <% end %>
  
<% end %>