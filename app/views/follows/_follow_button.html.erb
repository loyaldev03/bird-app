<% if current_user.nil? %>
  &nbsp;
<% elsif current_user.followed(user) %>
  <%= button_to "Unfollow", follow_path(current_user.followed(user)),
      method: :delete %>
<% elsif user == current_user %>
  (this is me)
<% else %>
  <%= button_to "Follow", follows_path, params: {target_id: user.id},
      method: :post %>
<% end %>