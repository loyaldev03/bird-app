<%= button_to text[1], follow_path(current_user.followed(object)),
      params: { text: text.to_s, classes: classes }, method: :delete, 
      class: "#{classes} unfollow-state", remote: true,
	  form_class: "follow-form follow-#{object.class.to_s}-#{object['id']} #{object.class.to_s == 'Topic' && object.locked ? 'follow-form-locked' : ''}", disabled: (object.locked if object.class.to_s == 'Topic')
%>