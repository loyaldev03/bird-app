var tracks = <%= raw @tracks.to_json %>;

setPlaylist(tracks, true);

$('.user-playlist-items li').removeClass('active');
$('#select-playlist-<%= @playlist_id %>').parent().addClass('active');
$('#jp-playlist-popover').removeClass('show');