var tracks = <%= raw @tracks.to_json %>;

if (tracks.length > 0) {
  // addToPlaylist(tracks, true);
  // setPlaylist(tracks, true);

  $("[id^='play-release-']").show().next('.jp-controls').css({'display': 'none'});
  $('#play-release-<%= @release_id %>').hide().next('.jp-controls').css({'display': 'inline-flex'});
}