<% if @post.parent_id.blank? %>

  $('#post_text').val('').focus();
  $('.emojionearea-editor').html('');
  var file = $("[type='file']");
  file.val('');
  file.next('span').remove();
  file.next('button').remove();
  
<% end %>